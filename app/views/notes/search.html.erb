<% notes.each do |note| %>
  <p>
    <strong><%= note.title %></strong>
  </p>

  <p>
    <%= markdown(note.body) %>
  </p>

  <p>
    <strong>Tags:</strong>
    <%= note.tags %>
  </p>
  ---------------------------------------
<% end %>